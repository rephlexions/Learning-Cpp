/*
Un libro ha un titolo, un autore e un numero di pagine. Un autore ha un
nome e un’età. Definire una lista di libri con metodi per aggiungere libri, per
stampare la lista dei libri, e per stampare la lista dei libri il cui autore ha più
di 50 anni.*/
#include <iostream>
using namespace std;

class Libro{
public:

protected:
  int pagine;
  char titolo[30];
};
int main (){
  return 0;
}
